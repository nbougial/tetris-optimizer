# Task 01: Project Structure Bootstrap

## Goal
Create the base Go project layout and directories.

## Steps
1. Initialize `go.mod`.
2. Create package folders (`cmd`, `internal/parser`, `internal/model`, `internal/solver`, `internal/output`, `testdata`).
3. Add placeholder files where needed.

## Done Criteria
- `go.mod` exists.
- Directory structure is committed.

## Depends On
- None.
