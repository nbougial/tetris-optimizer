# Task 02: CLI Contract

## Goal
Implement command-line argument validation.

## Steps
1. Accept exactly one argument (input file path).
2. Return `ERROR` if argument count is invalid.
3. Wire execution flow from `main`.

## Done Criteria
- CLI rejects invalid argument usage with `ERROR`.

## Depends On
- Task 01.
